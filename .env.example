# Copy to .env.local and fill in from Supabase → Project Settings → API
VITE_SUPABASE_URL=https://wfalosfgdkvahblxiytq.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_mchEd0Yn7cBJsq1sOKgKLw_TVfzgdnB

# Set to true to use the local sample map without logging in (optional)
# VITE_DEV_SKIP_AUTH=true

# Google sign-in: Supabase → Authentication → Providers → Google (enable).
# In Google Cloud Console, add authorized redirect URI from the Supabase Google provider page.
# Also add http://localhost:5173 under Supabase → Authentication → URL configuration.
